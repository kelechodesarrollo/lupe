<div class="jumbotron">
  <h2>Encontra todo lo que buscas en un solo sitio!</h2>
  <h2>Ikela Comerce te ofrece descuentos especiales</h2>
</div>
<div class="center">
<div class="row">
  <% @listings.each do |listing| %>
  <div class="col-md-3">
      <div class="thumbnail">
      <%= link_to (image_tag listing.image.url(:medium)), listing %>
      <div class="caption">
        <h3><%= listing.name %></h3>
        <p><%= number_to_currency(listing.price) %></p>
        <p><%= "Propietario: #{listing.user.name}"%></p>
      </div>
    </div>
       
       
  </div>
  <% end %> 
</div>
</div>
<br>

<% if user_signed_in? %>
  <%= link_to 'Nueva Fotito', new_listing_path, class: "btn btn-link" %>
<% end -%>